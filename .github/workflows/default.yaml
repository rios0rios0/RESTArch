name: 'default'

on:
  push:
    branches:
      - 'main'
    tags:
      - '*'
  pull_request:
    branches:
      - 'main'
  workflow_dispatch:

permissions:
  security-events: 'write'
  contents: 'write'

jobs:
  default:
    uses: 'rios0rios0/pipelines/.github/workflows/java-maven.yaml@main'
